# Dockerfile.milvus-init
FROM python:3.9-slim

# 安装pymilvus
RUN pip install --no-cache-dir pymilvus==2.5.2

# 设置工作目录
WORKDIR /app

# 复制初始化脚本
COPY secure_init.py /app/

# 设置环境变量
ENV PYTHONUNBUFFERED=1

# 默认命令
CMD ["python", "secure_init.py"]